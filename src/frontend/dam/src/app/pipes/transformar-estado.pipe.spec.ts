import { TransformarEstadoPipe } from './transformar-estado.pipe';

describe('TransformarEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformarEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
